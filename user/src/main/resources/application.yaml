spring:
  application:
    name: user-service
  # H2 database configs
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
server:
  port: 8082
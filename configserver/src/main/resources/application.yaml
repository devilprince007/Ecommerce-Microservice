spring:
  application:
    name: configserver

server:
  port: 8888
# configuration to connect to git hub
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/<user>/<repo-name>
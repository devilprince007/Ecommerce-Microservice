spring:
  application:
    name: configserver
  profiles:
    active: native

  rabbitmq:
    host: localhost
    port: 5672
    username: yash
    password: yash

  cloud:
    config:
      server:
        native:
          search-locations: file:///D:\PreprationCodes\SpringBootMicroservices\Microservices\Ecommerce-Microservice\configserver\src\main\resources\config

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8888

# configuration to connect to git hub
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/<user>/<repo-name>
